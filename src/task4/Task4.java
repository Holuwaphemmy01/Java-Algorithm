package task4;

import java.util.Arrays;
import java.util.stream.IntStream;


public class Task4 {

    public int [] arrayDifferent(int [] first, int [] second) {
        if (first.length < 1) throw new IllegalArgumentException();
        int [] response  = new int[first.length - second.length];

        for (int i = 0; i < first.length; i++) {

        }
        return first;
    }
}
